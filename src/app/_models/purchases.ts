export class Purchases {

    constructor(public title: string, public url: string, public fullName: string, public email: string, public value: number, public tablenumber: string, public addedon: string, public addedBy: string) {

    }
}